-record(node, {pid,
               monitor,
               port_no,
               proxy_port_no,
               node_type,
               protocol,
               highest_version,
               lowest_version,
               node_name,
               extra}).

